# Project Title

## Overview

This an example base how to create an Agent using LLM and functions,
Feel free to add comments or whatever that you consider to improve it.

```bash
git clone https://github.com/username/repository.git
cd repository
```

Install the required dependencies:

```bash
pip install -r requirements.txt
```

## Usage

To run the project, execute the following command:

```bash
python main.py
```
